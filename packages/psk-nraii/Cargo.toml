[package]
name = "psk-nraii"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish = false

# Bewusst OHNE Abhaengigkeiten - siehe den Modulkopf von src/lib.rs.
# QPM Regel 9.2 (Eigenständig in der Architektur, nicht in den Grundlagen)
# verlangt die UEBERNAHME der Grundlagen (Kanonisierung, Digestbildung,
# Identitaetsprojektion, Trace, Residuenbuchfuehrung), nicht ihren Nachbau.
# L0 hat nichts, was zu kanonisieren waere: Payload(N_0) ist leer. Die
# erste Abhaengigkeit auf psk-canon/psk-types entsteht mit L1, wo es
# erstmals eine Form zu fixieren gibt.
[dependencies]
